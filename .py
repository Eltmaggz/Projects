def greet(name):
    return f"Hello, {name}!"
print(greet("World"))
# This is a simple greeting function
# that takes a name as input and returns a greeting message.
# The function is designed to be easy to use and understand.
# The function can be extended to include more features in the future.